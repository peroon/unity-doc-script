<signature>
preset
</signature>
Set or get an XML string of "input/value" pairs (settings the preset rebuilds the textures).
This allows to quickly save the set of a ProceduralMaterial's settings and to export them or to re-apply them later on.