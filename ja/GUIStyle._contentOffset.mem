<signature>
contentOffset
</signature>
コンテンツに適用するピクセル単位のオフセットを設定する
CONVERTEXAMPLE
BEGIN EX
	// Prints the value of contentOffset inside customStyles[0]
	
	function OnGUI() {
		if(GUI.skin.customStyles.Length > 0)
			Debug.Log(GUI.skin.customStyles[0].contentOffset);
	}
END EX