<signature>
shift
</signature>
Shift キーが押されたかを返す（読み込み専用）
任意の Shift キーが押下されたとき true を返します。
CONVERTEXAMPLE
BEGIN EX
	// Shift キーが押下されたかを検知
	function OnGUI() {
		var e : Event = Event.current;
		if (e.shift) {
			Debug.Log("Shift キーが押下されました");
		}
	}
END EX
