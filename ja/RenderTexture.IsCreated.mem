<signature>
IsCreated()
</signature>
実際にRenderTextureオブジェクトを生成しているか確認する。
RenderTexture constructor does not actually create the hardware texture;
by default the texture is created the first time it is set ::ref::active.
@@IsCreated@@ returns /true/ if the hardware resources for this render
are created.

SA: ::ref::Create, ::ref::Release functions.