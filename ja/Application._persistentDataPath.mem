<signature>
persistentDataPath
</signature>
永続的なデータディレクトリのパスを返す（読み取り専用）。
The value is a directory path where data expected to be kept between runs can be stored.
CONVERTEXAMPLE
BEGIN EX
	// print the path to the persistent data folder
	print (Application. persistentDataPath);
END EX