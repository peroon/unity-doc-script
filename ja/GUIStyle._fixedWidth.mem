<signature>
fixedWidth
</signature>
0でない場合、このスタイルでレンダリングされるすべてのGUI要素は、ここで指定した幅を持つことになる
CONVERTEXAMPLE
BEGIN EX
	// Prints the value of fixedWidth of customStyles[0]
	
	function OnGUI() {
		if(GUI.skin.customStyles.Length > 0)
			Debug.Log(GUI.skin.customStyles[0].fixedWidth);
	}
END EX