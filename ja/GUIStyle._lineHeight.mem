<signature>
lineHeight
</signature>
テキストの1行の高さをピクセル単位で返す（読み込み専用）
CONVERTEXAMPLE
BEGIN EX
	// Prints the lineHeight value of customStyles[0]

	function OnGUI() {
		if(GUI.skin.customStyles.Length > 0)
			Debug.Log(GUI.skin.customStyles[0].lineHeight);
	}
END EX