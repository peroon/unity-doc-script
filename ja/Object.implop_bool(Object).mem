<signature>
implop_bool(Object)
</signature>
オブジェクトは存在するか？
以下の二つのサンプルは同じ結果が得られます。
CONVERTEXAMPLE
BEGIN EX
	// Rigidbody がこの Transform にアタッチされているかチェックします。

	if (rigidbody) 
	   Debug.Log("Rigidbody がこの Transform にアタッチされています。");
END EX
...is the same as this...
CONVERTEXAMPLE
BEGIN EX
	// Rigidbody がこの Transform にアタッチされているかチェックする別の方法です。

	if (rigidbody != null)
	   Debug.Log("Rigidbody がこの Transform にアタッチされています。");
END EX
