<signature>
width
</signature>
ピクセル単位でのテクスチャの幅を返す（読み込み専用）
CONVERTEXAMPLE
BEGIN EX
	// Print texture size to the Console
	var texture : Texture;
	function Start () {
		print("Size is " + texture.width + " by " + texture.height);
	}
END EX