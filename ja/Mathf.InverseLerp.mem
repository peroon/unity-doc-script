<signature>
InverseLerp(float,float,float)
</signature>
2 つの値のLerp関数のパラメーターを算出する。
CONVERTEXAMPLE
BEGIN EX
	var walkSpeed = 5.0;
	var runSpeed = 10.0;
	var speed = 8.0;

	function Start() {
		// parameter is now 3 / 5
		var parameter : float = Mathf.InverseLerp(walkSpeed, runSpeed, speed);
	}
END EX